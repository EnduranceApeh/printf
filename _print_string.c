#include "main.h"

/**
 * _print_string - a function to print the incoming value as a char
 * @str: takes in a value of characters and
 * turn it to a void because we don't wanna use it here
 * Return: returs an integer
 */

int _print_string(char *str)
{
	(void)str;
	return (0);
}
